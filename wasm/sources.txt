# sukkal's own C sources that compile for WASM, one per line, relative to
# the package root. Consumed by build-wasm.sh.
#
# Empty at Phase 0 on purpose. A file joins this list once it no longer
# reaches POSIX, http11c or libcurl directly — see docs/wasm-plan.md for
# the three seams and the order they get cut in. store.c is first.
